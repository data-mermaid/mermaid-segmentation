# gradio 6.x: 5.0.0 imports HfFolder, removed in huggingface_hub 1.x (pulled in by mermaidseg).
# Keep in sync with sdk_version in README.md frontmatter.
gradio>=6.0
# Pinned to feat/concept-loss-label-norm-coralscapes (SHA c36f1ff): this checkpoint
# was trained with that branch's model code (activated [0,1] concept hidden_states +
# token-grid inference). main returns raw concept logits and would break the overlays.
# TODO: repin to a main tag once that branch is merged.
mermaidseg @ git+https://github.com/data-mermaid/mermaid-segmentation.git@c36f1ffb98144f8043da30cece14912b7bbc441c
