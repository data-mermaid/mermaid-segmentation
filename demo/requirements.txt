# NOTE: do NOT list gradio/spaces/huggingface_hub here — the Space platform manages
# them (gradio is locked by sdk_version in README.md frontmatter; pinning gradio or
# spaces in requirements breaks the ZeroGPU runtime). torch stays unpinned so the
# ZeroGPU-preinstalled build (2.11) is kept.
#
# Pinned to demo/demo-update-images (SHA 8756d54): GroupNorm DPT head + demo shims
# that match the current ViT-L LoRA CBM checkpoint (79 classes / 704 concepts).
# torch stays unpinned above so ZeroGPU keeps its preinstalled build.
mermaidseg @ git+https://github.com/data-mermaid/mermaid-segmentation.git@8756d5411c38c3ef4b6fde2a7abbc7a329aca59e
