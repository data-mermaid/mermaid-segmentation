[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mermaidseg"
version = "0.1.0"
description = "A package for the segmentation of MERMAID coral data."
requires-python = ">=3.11"
dependencies = [
    "albumentations",
    "boto3",
    "numpy>=1.24,<2",
    "pandas",
    "pillow",
    "requests",
    "torch>=2.4,<2.8",      
    "transformers",
    "datasets",
    "s3fs",
    "segmentation-models-pytorch",
    "mlflow",
    "pyyaml",
    "tqdm",
    "torchmetrics",
    "anytree",
    "openpyxl",
    "ipykernel>=7.2.0",
    "matplotlib>=3.10.8",
    "huggingface-hub>=0.36.2",
    "seaborn>=0.13.2",
    "opencv-python-headless>=4.11.0.86",
    "sagemaker-mlflow>=0.2.0",
    "requests-auth-aws-sigv4>=0.7",
    "safetensors>=0.4.0",
]

[project.optional-dependencies]
notebooks = [
    "jupyter",
    "ipykernel",
    "ipywidgets",
    "wandb>=0.25.0",
]
scraping = [
    "selenium",
    "webdriver-manager",
]

[dependency-groups]
tests = [
    "pytest>=8.0",
    "pytest-cov>=4.0",
]
lint = [
    "ruff>=0.15.8",
    "pre-commit>=3.0",
]
notebooks-ci = [
    "nbqa>=1.9",
    "ruff>=0.15.8",
]
dev = [
    {include-group = "tests"},
    {include-group = "lint"},
    "docformatter>=1.7.7",
]

[tool.setuptools]
packages = { find = { where = ["."], include = ["mermaidseg*"] } }

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "C4", "RET", "SIM", "PD"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"**/*.ipynb" = [
    "F821",   # undefined names — common in exploratory notebook cells
    "B018",   # useless expression — used to display values in notebooks
    "E501",   # line too long — handled by docformatter; notebook cells are hard to wrap
    "UP031",
    "PD003", # pandas-vet: prefer isna — wrong for Ibis .isnull() / SQL NULL
]

[tool.ruff.lint.isort]
known-first-party = ["mermaidseg"]

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--strict-markers",
    "--strict-config",
    "-ra",
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
]

[tool.coverage.run]
source = ["mermaidseg"]
omit = ["tests/*", "*/conftest.py"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]
